language: python
dist: xenial
python: 2.7
before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq libopenblas-base
script: pytest test/test_prior_external.py
packages:
  - libopenblas-base